# Local scratch / preview downloads (not part of the build)
.muse-previews/

# Local preview screenshots (not part of the build)
.preview/

# Secrets / credentials — never commit these (important for Shopify/API keys later)
.env
.env.*
*.pem
*.key
*.p12
secrets.json
credentials.json

# Dependencies / build noise
node_modules/
.DS_Store
